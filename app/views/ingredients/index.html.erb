<ul>
  <% @ingredients.each do |ingredient| %>
    <li><%= ingredient.quantity %> <%= ingredient.measurement %> - <%= ingredient.item.name %></li>
  <% end %>
</ul>

<!-- <li>link_to ingredient.name, recipe_ingredient_url(ingredient) </li> -->
