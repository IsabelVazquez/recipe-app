<%= form_for (@item) do |f| %>
  <div class="fields_with_errors">
    <% unless @item.errors.empty? %>
      <%= @item.errors.full_messages %>
    <% end %>
  </div>

  <%= f.label :name %><br>
  <%= f.text_area :name %><br>

  <%= f.submit %>
<% end %>
